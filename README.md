# FSC Open Docs

## Contributing

We are using [uv](https://docs.astral.sh/uv/) for environment management. To get started, you can sync uv and run the dev server:

```bash
git clone https://github.com/firstsouthcarolina/FSCOpenDocs
cd FSCOpenDocs
uv sync
uv run zensical serve
```

